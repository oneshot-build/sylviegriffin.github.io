---
const steps = [
  {
    number: "01",
    title: "Connect",
    description: "Book a session to share where you are right now and what you're seeking. This is a safe space to explore your dreams, fears, and the life you truly want.",
    duration: "First Session",
  },
  {
    number: "02",
    title: "Discover",
    description: "Through coaching, we'll uncover your authentic desires and identify the beliefs holding you back. You'll begin to see your unique purpose more clearly.",
    duration: "Ongoing",
  },
  {
    number: "03",
    title: "Transform",
    description: "As you align with your true self, everything shifts. We'll use coaching, yoga, reiki, and other practices to support your journey toward a fulfilling, abundant life.",
    duration: "Your Journey",
  },
];
---

<section id="how-it-works" class="process-section">
  <div class="container">
    <header class="process-header" data-reveal>
      <span class="section-subtitle">The Journey</span>
      <h2 class="section-title balance">
        Your path to purpose
      </h2>
      <p class="process-intro">
        Everyone's journey is unique, but transformation follows a natural rhythm.
        Here's what working together typically looks like.
      </p>
    </header>

    <div class="process-steps">
      {steps.map((step, index) => (
        <div
          class="step-card"
          data-reveal
          style={`transition-delay: ${index * 150}ms`}
        >
          <div class="step-header">
            <span class="step-number">{step.number}</span>
            <span class="step-duration">{step.duration}</span>
          </div>
          <h3 class="step-title">{step.title}</h3>
          <p class="step-description">{step.description}</p>
        </div>
      ))}
    </div>

    <div class="process-connector" aria-hidden="true">
      <svg viewBox="0 0 100 10" preserveAspectRatio="none">
        <line x1="0" y1="5" x2="100" y2="5" stroke="var(--border)" stroke-width="1" stroke-dasharray="4 4"/>
      </svg>
    </div>
  </div>
</section>

<style>
  .process-section {
    padding: var(--space-3xl) 0;
    background: var(--bg-primary);
  }

  .process-header {
    text-align: center;
    max-width: 600px;
    margin: 0 auto var(--space-2xl);
  }

  .section-subtitle {
    display: block;
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--accent);
    margin-bottom: var(--space-sm);
  }

  .section-title {
    font-family: var(--font-display);
    font-size: var(--text-4xl);
    font-weight: var(--weight-normal);
    color: var(--text-primary);
    margin-bottom: var(--space-md);
  }

  .process-intro {
    font-size: var(--text-lg);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .process-steps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
    position: relative;
  }

  .step-card {
    padding: var(--space-lg);
    background: var(--bg-secondary);
    border-radius: var(--radius-xl);
    border: 1px solid var(--border);
    transition: all var(--duration-normal) var(--ease-calm);
  }

  .step-card:hover {
    border-color: var(--accent-muted);
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .step-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-md);
  }

  .step-number {
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    font-weight: var(--weight-medium);
    color: var(--accent);
  }

  .step-duration {
    font-size: var(--text-xs);
    font-weight: var(--weight-medium);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wider);
    color: var(--text-muted);
    padding: var(--space-2xs) var(--space-xs);
    background: var(--bg-primary);
    border-radius: var(--radius-sm);
  }

  .step-title {
    font-family: var(--font-body);
    font-size: var(--text-xl);
    font-weight: var(--weight-semibold);
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
  }

  .step-description {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    max-width: none;
  }

  .process-connector {
    display: none;
  }

  @media (max-width: 1024px) {
    .process-steps {
      grid-template-columns: 1fr;
      max-width: 500px;
      margin: 0 auto;
    }
  }
</style>
