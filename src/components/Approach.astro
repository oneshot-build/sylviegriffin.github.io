---
const services = [
  {
    title: "Life Coaching",
    description: "One-on-one sessions to help you discover your unique purpose, overcome limiting beliefs, and create a life aligned with your authentic self. This is where transformation begins.",
  },
  {
    title: "Yoga & Movement",
    description: "Reconnect with your body and breath through personalized yoga practices. Learn to listen to the wisdom your body has been trying to share with you.",
  },
  {
    title: "Reiki Healing",
    description: "Energy healing sessions to release blockages, restore balance, and support your spiritual journey. Experience the profound peace that comes from alignment.",
  },
  {
    title: "Conscious Travel",
    description: "Transformative travel experiences in Brazil that combine coaching, yoga, and cultural immersion. Step out of your routine and into possibility.",
  },
];
---

<section id="approach" class="approach-section">
  <div class="container">
    <div class="approach-grid">
      <div class="approach-intro" data-reveal>
        <span class="section-subtitle">Services</span>
        <h2 class="section-title balance">
          A holistic approach to finding your purpose
        </h2>
        <p class="approach-lead">
          True transformation happens when we address mind, body, and spirit together.
          My services are designed to work in harmony, helping you discover who you're meant
          to be and supporting you every step of the way.
        </p>
        <p class="approach-text">
          I believe <strong>everyone has a unique mission on earth, and when you find yours and
          serve it, abundance flows naturally.</strong> My role is to help you reconnect with your
          authentic self and give you the tools to live the fulfilling life you're meant to have.
        </p>
      </div>

      <div class="approach-differentiators">
        {services.map((item, index) => (
          <div
            class="differentiator-card"
            data-reveal
            style={`transition-delay: ${index * 100}ms`}
          >
            <h3 class="differentiator-title">{item.title}</h3>
            <p class="differentiator-text">{item.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .approach-section {
    padding: var(--space-3xl) 0;
    background: var(--bg-primary);
  }

  .approach-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-2xl);
    align-items: start;
  }

  .approach-intro {
    position: sticky;
    top: var(--space-2xl);
  }

  .section-subtitle {
    display: block;
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--accent);
    margin-bottom: var(--space-sm);
  }

  .section-title {
    font-family: var(--font-display);
    font-size: var(--text-4xl);
    font-weight: var(--weight-normal);
    color: var(--text-primary);
    margin-bottom: var(--space-lg);
  }

  .approach-lead {
    font-size: var(--text-lg);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-md);
  }

  .approach-text {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .approach-text strong {
    color: var(--text-primary);
  }

  .approach-differentiators {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .differentiator-card {
    padding: var(--space-lg);
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    transition: all var(--duration-normal) var(--ease-calm);
  }

  .differentiator-card:hover {
    border-color: var(--accent-muted);
    transform: translateX(4px);
  }

  .differentiator-title {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    font-weight: var(--weight-semibold);
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
  }

  .differentiator-text {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    max-width: none;
  }

  @media (max-width: 1024px) {
    .approach-grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .approach-intro {
      position: static;
    }
  }
</style>
