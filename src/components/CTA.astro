---

---

<section id="book" class="cta-section">
  <div class="container">
    <div class="cta-content" data-reveal>
      <h2 class="cta-headline balance">
        Your life is waiting. Are you ready to listen?
      </h2>

      <p class="cta-text">
        If you've read this far, your heart is already speaking to you. The question isn't whether
        you deserve a fulfilling life—you absolutely do. The question is: are you ready to claim it?
      </p>

      <p class="cta-text">
        Book a coaching session and take the first step toward discovering your unique purpose
        and creating a life that fills you with joy and abundance.
      </p>

      <div class="cta-actions">
        <a href="https://wa.me/5521960118962?text=Hi%20Sylvie%2C%20I%27d%20like%20to%20book%20a%20coaching%20session" class="btn btn-primary btn-large" target="_blank" rel="noopener noreferrer">
          Book your session
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
            <path d="M4 10h12M12 6l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>

      <p class="cta-reassurance">
        Transform your life. Discover your mission. Live with purpose and abundance.
      </p>
    </div>

    <!-- The Breathing Line -->
    <div class="cta-divider">
      <div class="divider-breathing"></div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding: var(--space-3xl) 0;
    background: var(--bg-secondary);
  }

  .cta-content {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
  }

  .cta-headline {
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: var(--weight-normal);
    font-style: italic;
    color: var(--text-primary);
    margin-bottom: var(--space-lg);
    line-height: var(--leading-tight);
  }

  .cta-text {
    font-size: var(--text-lg);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-md);
    max-width: none;
  }

  .cta-actions {
    margin-top: var(--space-xl);
    margin-bottom: var(--space-md);
  }

  .btn-large {
    padding: var(--space-md) var(--space-xl);
    font-size: var(--text-base);
  }

  .btn-large svg {
    transition: transform var(--duration-fast) var(--ease-calm);
  }

  .btn-large:hover svg {
    transform: translateX(4px);
  }

  .cta-reassurance {
    font-size: var(--text-sm);
    color: var(--text-muted);
    max-width: none;
  }

  .cta-divider {
    margin-top: var(--space-2xl);
  }

  @media (max-width: 768px) {
    .cta-headline {
      font-size: var(--text-2xl);
    }

    .cta-text {
      font-size: var(--text-base);
    }
  }
</style>
